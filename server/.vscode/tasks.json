{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Install deps (pnpm or npm)",
			"type": "shell",
			"command": "pwsh",
			"args": [
				"-NoProfile",
				"-Command",
				"if (Get-Command pnpm -ErrorAction SilentlyContinue) { pnpm install } else { npm install }"
			],
			"isBackground": false,
			"problemMatcher": [],
			"group": "build"
		},
		{
			"label": "Build TS",
			"type": "shell",
			"command": "pwsh",
			"args": [
				"-NoProfile",
				"-Command",
				"pnpm -v; if (Get-Command pnpm -ErrorAction SilentlyContinue) { pnpm build } else { npm run build }"
			],
			"isBackground": false,
			"problemMatcher": [
				"$tsc"
			],
			"group": "build"
		},
		{
			"label": "Install deps (pnpm or npm)",
			"type": "shell",
			"command": "pwsh",
			"args": [
				"-NoProfile",
				"-Command",
				"if (Get-Command pnpm -ErrorAction SilentlyContinue) { pnpm add he } else { npm install he }"
			],
			"isBackground": false,
			"group": "build"
		},
		{
			"label": "Install he package",
			"type": "shell",
			"command": "pwsh",
			"args": [
				"-NoProfile",
				"-Command",
				"if (Get-Command pnpm -ErrorAction SilentlyContinue) { pnpm add he } else { npm install he }"
			],
			"isBackground": false,
			"group": "build"
		}
	]
}